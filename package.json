{
  "name": "ai-resume",
  "version": "1.0.0",
  "private": true,
  "description": "AI Resume Builder - 智能简历制作工具",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix api && npm run build --prefix web",
    "install:all": "cd api && npm install && cd ../web && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
